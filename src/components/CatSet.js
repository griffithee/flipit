import React, { PropTypes } from 'react'

const propTypes = {}

const defaultProps = {}

const CatSet = props => (
    <div>
        
    </div>
)

CatSet.propTypes = propTypes

CatSet.defaultProps = defaultProps

export default CatSet
